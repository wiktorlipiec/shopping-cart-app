import React, { Component } from 'react';
import Header from '../../container/Header';

class Home extends Component {
    render(){
        return(
            <Header />
        );
    }
}

export default Home;